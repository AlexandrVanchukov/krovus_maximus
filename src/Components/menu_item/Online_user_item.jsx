import React from 'react';
import Button from "../UI/Button/Button";

const OnlineUserItem = (props) => {
    return (
        <div>
            {props.user}
        </div>
    );
};

export default OnlineUserItem;